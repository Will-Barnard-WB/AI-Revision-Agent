# Tool call guards are now handled by the built-in ToolCallLimitMiddleware.
# See agent_factory.py and config.yaml -> limits for configuration.
#
# This file is intentionally empty — kept so existing imports don't break.